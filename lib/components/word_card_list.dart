import 'package:flutter/widgets.dart';

class WordListCard extends StatelessWidget {
  const WordListCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
